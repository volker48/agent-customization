---
description: List active and recent Pi implementation jobs for this workspace
argument-hint: ""
---

Run this exact command from the current project and report the compact job table to the user:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/pi-companion.mjs" status
```

Point the user at `/pi:result latest` or `/pi:result <job-id>` for final output and evidence.
