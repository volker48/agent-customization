---
description: Verify local Node and Pi RPC setup for future Pi implementation delegation
argument-hint: ""
---

Run this exact command from the current project and report the result to the user:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/pi-companion.mjs" setup
```

Summarize any failed checks and the model guidance. Do not edit files while running setup.
