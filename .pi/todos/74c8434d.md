{
  "id": "74c8434d",
  "title": "webfetch: add Readability+Turndown HTML→markdown conversion",
  "tags": [
    "webfetch",
    "extension"
  ],
  "status": "closed",
  "created_at": "2026-06-08T19:22:24.098Z"
}

Implemented Readability + Turndown HTML-to-markdown conversion for `webfetch`.

Done:
- Added runtime deps: @mozilla/readability 0.6.0, turndown 7.2.4, turndown-plugin-gfm 1.0.2, linkedom 0.18.12 after checking npm registry latest metadata.
- Added default-on `text/html` conversion with `raw: true` opt-out and 5 MiB conversion cap.
- Added Readability article extraction, full-page Turndown fallback, GFM table support, and title/source/byline/site header.
- Preserved probe/raw/non-HTML/oversized behavior and degraded to raw HTML on conversion failures.
- Added conversion diagnostics: `converted`, `conversionMethod`, `originalHtmlBytes`.
- Added focused webfetch tests for article conversion, GFM tables, thin HTML fallback, converter throw fallback, raw opt-out, oversized skip, and converted-output maxChars truncation.
- Updated README deployment caveat for runtime dependencies.

Validation:
- `pnpm vitest run tests/webfetch.test.ts` passes.
- Targeted `tsc` for webfetch + tests passes.
- Targeted `oxlint` for webfetch + tests passes.
- `pnpm format:check` passes.

Known unrelated existing failures:
- `pnpm typecheck` and `pnpm test` fail because `pi-extensions/handoff/index.js` is missing for handoff tests.
- Full `pnpm lint` terminated abnormally/OOM, but targeted oxlint passed.

Follow-up fix:
- Replaced top-level converter imports with lazy `createRequire(realpathSync(import.meta.url))` loading so pi can load the extension from `~/.pi/agent/extensions` without failing module resolution before startup.
- Verified in subprocess: `pi --no-extensions --extension /Users/mmccurdy/.pi/agent/extensions/webfetch.ts --no-skills --no-prompt-templates --no-themes --no-context-files --offline` exits without extension load errors.
- Verified default pi startup subprocess with discovered extensions also exits without the previous readability error.
